version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  apt_packages:
    - doxygen
    - graphviz
  jobs:
    pre_build:
      - cd Docs && doxygen Doxyfile

sphinx:
  configuration: Docs/conf.py

python:
  install:
    - requirements: Docs/requirements_docs.txt
